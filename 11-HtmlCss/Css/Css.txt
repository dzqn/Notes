*   User agent stylesheet
    normalize.css

*   Blok elementler : display:block
    div,p

    inline elementler : display:inline
    a,b,

    display
        inline: inline ol
        block: block olarak davran
        inline-block: inline olarak devam et ama block element önzellikleriin de al.
        initial
        none
        inherit
        list-item
        table,table-row,table-cell ...
        flex,inline-flex

*   position (left,right,top,bottom) un kullanılabilmesi için verilir.
    relative
    ablolute
    fixed
    static

    z-index --> position değeri verlmesi gerekiyor. Yoksa çalışmaz.

*   1m = font-size
*   max-width

*   box-sizing: border-box 
                content-box

*   1em = font-size
    1rm = route em --> em kapsayıcısındaki font-size'a bakar. rem ise route da(html tag) da yer alan font-size'a bakar
    vw --> tüm genişlik %100
    vh --> tüm yükseklik  %100

*   visibility:hidden --> dom'dan silmez. Ekrandaki pozisyonu da silinmez. Ekranda bulunduğu yerde sadece beyaz bir kutu görünür.
    display:none      --> dom'dan silme. Ekranda ki pozisyonu silinir. Sanki ekranda hiç yokmuş gibi davranır.

